export default {
  /*
  name: String, // 'RESTAdapter',
  message: function message($data, $isBinary) {
    try {
      const data = $data
      let message
      const dataString = JSON.stringify(data.toString())
      message = this.messages[dataString]
      return message
    }
    catch($err) {}
    return
  },
  messages: {
    // messageName: Function $messageListener,
    'get': function getMessage($webSocket, $data, $isBinary) {
      $webSocket.send("GET")
      return true
    },
  },
  */
}